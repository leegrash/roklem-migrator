{
  "RazorPages": {
    "StaticContent": {
      "ClientCache": {
        "CacheControl": "public",
        "CacheControlMode": "UseMaxAge",
        "CacheControlMaxAge": "7.00:00:00"
      }
    }
  }
}

<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net6.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Mvc" Version="2.2.0" /> <!-- Changed version from 6.0.0 to 2.2.0 to match available versions -->
  </ItemGroup>

</Project>

<!-- 
1. Updated the PackageReference for Microsoft.AspNetCore.Mvc from version `6.0.0` to `2.2.0` as it was the nearest found version in the error output.
2. The project SDK was already correctly set to `Microsoft.NET.Sdk.Web`.
3. The TargetFramework was correctly set to `net6.0`. 
-->